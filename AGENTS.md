# AGENTS.md

## Cursor Cloud specific instructions

This repository (`hacktricks-cloud`) is currently an empty/placeholder repository containing only a `README.md`. There is no application code, no dependencies, no build system, no tests, and no services to run.

- **No package manager files** (no `package.json`, `requirements.txt`, `Gemfile`, etc.)
- **No Dockerfiles or docker-compose** configurations
- **No source code** in any language
- **No CI/CD pipelines** configured

Since there is nothing to build, test, lint, or run, no special dev environment setup is required. Future agents should re-evaluate this file once actual code is added to the repository.
